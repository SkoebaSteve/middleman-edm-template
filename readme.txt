edm template based on middleman, with lots of special features:

- inline css
- sass support(with nesting)
- seperate your html with partials
- option to build and and ship everything

to run the server, use "bundle middleman exec".

to build the files, use "bundle middleman exec". Output will be in the "build folder"